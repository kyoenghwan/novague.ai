'use client';

import MainPage from '@/pages/MainPage';

/**
 * Next.js App Router entry point.
 * Actual logic and layout are managed in the @/pages layer.
 */
export default function Home() {
  return <MainPage />;
}
